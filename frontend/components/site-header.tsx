import { SiteNav } from "@cmsjs/components/site-nav";

// Placeholder site header — extend with the site's brand and design.
export function SiteHeader() {
  return (
    <header>
      <SiteNav />
    </header>
  );
}
